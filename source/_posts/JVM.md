---
title: JVM
date: 2024-05-26 01:21:46
---
## 字节码文件详解
### 字节码文件的组成部分
#### 基本信息
##### 魔数

- 文件时无法通过文件扩展名来确定文件类型的， 文件扩展名可以随便修改，不影响文件的内容
- java字节码文件中，将头文件称为magic魔数 （CAFEBABE）

##### 主副版本号

- 指的是编译字节码文件的JDK版本号 
- 版本号的作用主要是判断当前字节码的版本和运行时的jdk是否兼容

##### 访问标识

标识是类还是接口，注解，枚举，模块


#### 常量池

保存了字符串常量，类或者接口名，字段名，主要在字节码指令中使用

作用： 避免相同的内容重复定义，节省空间

#### 方法

操作数栈 是临时存放数据的地方，局部变量表示存放方法中的局部变量的位置

```
iconst_0 向操作数栈中添加常量0
istore_1 将操作数栈顶端的数弹出到 局部变量表数组中的 下标为1的地方 
iload_1 将局部变量表数组中的 下标为1 的地方里的值存放到操作数栈中
iconst_1 操作数栈中添加1
iadd 将操作数栈顶部的两个数据（弹出）进行累加，结果放入栈中
istore_2 将操作数栈顶端的数弹出到 局部变量表数组中的 下标为2的地方 
return
```


```
i= i++ 流程
iconst_0
istore_1
iload_1
iinc 1 by 1 将局部变量表中下标为1的位置的数 + 1
istore_1
return

```

由此可以看出 在i++时，由于操作数栈中的数值是0 因此在store的时候会覆盖掉局部变量表数组中的数值1，所以结果是0

```
i= ++i 流程
iconst_0
istore_1
iinc 1 by 1 将局部变量表中下标为1的位置的数 + 1 
iload_1 此时 将会往操作数栈里存放（不弹出）的数值 为 1
istore_1 将操作数栈顶端的数弹出到 局部变量表数组中的 下标为1的地方 
return

```

由此可以看出 在++i时，由于先在局部变量表中加了数据1， 再将1存放到操作数栈中，最后在store到局部变量表数组，下标为1的地方

#### 虚方法表（实现多态的基础）

### 类的生命周期

描述了一个类加载，使用，卸载的整个过程

#### 生命周期的概述

五个阶段
加载 连接 初始化 使用 卸载（垃圾回收）

有的会分成7个阶段，是将连接阶段分为：验证，准备，解析

#### 加载阶段

1 加载阶段的第一步是根据类的全限定名通过不同的渠道以二进制的方式获取字节码信息。程序员可以使用java代码拓展不同的渠道
- 本地文件：磁盘上的字节码文件
- 动态代理生成：程序运行时使用动态代理生成
- 通过网络传输的类

2 类加载器加载完类之后，java虚拟机会将字节码中的信息保存到方法区中（方法区是一个虚拟概念）

3 虚拟机在收集到字节码文件之后，生成一个InstanceKlass对象，保存类的所有信息，里面还包含实现特定功能--比如多态的信息

4 同时，java虚拟机还会再**堆**中生成一份与方法去中数据类似的java.lang.Class对象 作用是在java代码中区获取类的信息一集存储静态字段的数据（JDK8及以后）

##### 查看内存中的对象
JDK自带的hsdb java -cp -sa.jdi.jar sun.jvm.hotspot.HSDB

jps 可以展示所有的java进程和对应的进程id
[[加载阶段生命周期图 1]]

#### 连接阶段

##### 验证

验证内容是否满足《java虚拟机规范》，不需要程序员参与
主要包含四部分：
- 文件格式验证，比如文件是否以0XCAFEBABE开头，主次版本号是否满足当前Java虚拟机版本的要求
- 元信息验证，比如类必须有父类（super不能为空）
- 验证程序执行指令的予以，比如方法内的指令执行到一般强行跳转到其他方法中去
- 符号引用验证，例如是否访问量其他类中的private方法

版本号的检查：主版本号不能高于运行环境的主版本号，如果主版本号相等，副版本号也不能超过
##### 准备

**为静态变量分配内存并设置初始值**

比如
``` Java
public class Student{
public static int value = 1;
}
```

此时，会在堆里的Student.class创建一个value的区域，并且给默认值0（此时还没初始化）

但是，如果使用final修饰的话，准备阶段直接会将代码中的值进行赋值

``` Java
public class Student{
public static final int value = 1;
}
```
此时，会在堆里的Student.class创建一个value的区域，并且给默认值1

##### 解析

将常量池中的符号引用替换成指向内存的直接引用
直接引用指的是：不再使用编号（如 # 23）, 而是使用内存中地址进行访问具体的数据

#### 初始化阶段

执行静态代码块和静态变量的赋值

- 初始化阶段会执行静态代码块中的代码，并且为静态变量赋值
- 初始化阶段会执行字节码文件中clinit部分的字节码指令（clinit 可理解为class init）

在clinit方法中的执行顺序与java中的编写的顺序是一致的。

```java
public class Demo1 {  
public static int value = 1;  
  
static {  
value = 2;  
}  
  
public static void main(String[] args) {  
  
}  
}
```

```
iconst_1
putstatic #2 <Demo1.value : I>
iconst_2
putstatic #2 <Demo1.value : I>
return
```

一下几种方式会导致<font color="#ffff00">类的初始化</font>：
1. 访问一个类的静态变量或者静态方法，注意变量是final修饰的并且右边是常量则不会触发初始化
2. 调用Class.forName(String className)
3. new一个该类的对象时
4. 执行Main方法的当前类

-XX:+TraceClassLoading 参数可以打印出加载并初始化的类

clinit指令在特定的情况下不会出现，比如：如下几种情况是不会进行初始化指令执行的：
1. 无静态代码块且无静态变量赋值语句
2. 有静态变量的声明，但是没赋值语句 e.g. public static int a;
3. 静态变量的定义使用final关键字，这类变量会在准备阶段直接进行初始化 e.g. public static final int a = 10;

访问父类的静态变量，只需要初始化父类就行，不会初始化子类
``` java
public class Demo1 {  
  
public static void main(String[] args) {  
  
System.out.println(B02.a);  
}  
}  
  
class A02{  
static int a = 0;  
static {  
a = 1;  
}  
}  
  
class B02 extends A02{  
  
static {  
a = 2;  
}  
}
```

因此输出的是 1

注意：
1. 数组的创建不会导致元素中的类进行初始化：因为仅仅是分配空间，并不会对类进行初始化，即使有静态变量或者静态代码块
2. final修饰的变量如果赋值的内容需要执行指令才能得出结果，会执行clinit方法进行初始化


### 类加载器（ClassLoader）

类加载器是java虚拟机提供给应用程序去实现获取类和接口字节码数据的技术
类加载器只参与加载过程中的字节码获取并加载到内存这一部分过程

##### 类加载器的分类

主要分为两类，以类似和java代码中实现的，一类是java虚拟机底层源代码实现

JDK8及之前的版本中默认的类加载器有如下几种：
1 **虚拟机底层实现**
启动类加载器Bootstrap

2 **java**
扩展类加载器Extension 允许扩展java中比较通用的类
应用程序类加载器 Application
###### 虚拟机底层实现（启动类加载器）

是由Hotspot虚拟机提供的，使用C++编写的类加载器
默认加载Java安装目录下的jre/lib下的类文件，比如rt.jar, tools.jar
``` java
import java.io.IOException;  
  
public class BootstrapClassLoaderDemo {  
public static void main(String[] args) throws IOException {  
ClassLoader classLoader = String.class.getClassLoader();  
System.out.println(classLoader);  
  
System.in.read();  
}  
}

```

classLoader 为空，说明无法获取Bootstrap（毕竟Bootstrap是底层的逻辑，如果上层能随便获取，安全性也无法保证）

通过启动类加载器去加载用户jar包
使用参数进行扩展
推荐，使用-Xbootclasspath/a:jar包目录/jar包名 进行扩展
例如： -Xbootclasspath/a:D:/jvm/jar/jvm-1.0-classloader.jar
###### java代码实现 （扩展类加载器和应用程序类加载器）
jdk中默认提供或者自定义
继承自抽象类ClassLoader

<font color="#ffff00">**扩展类加载器和应用程序类加载器都是JDK中提供的，使用Java编写的类加载器**</font>
他们的源码都位于sun.misc.Launcher中，是一个静态内部类。继承自URLClassLoader
[[扩展类加载器和应用程序类加载器的继承关系 1]]

<font color="#ffff00">若要通过扩展类加载器去加载用户jar包</font> ：
指令 -Djava.ext.dirs=D:\\jdk1_8\\jre\lib\\ext;D:\\jvm\\jar
可以看出 扩展类加载器加载的是java安装目录/jre/lib/ext下的类文件 扩展类加载器允许扩展Java比较通用的类

**应用程序类加载器 加载的是classpath下的类文件(既可以加载项目中创建的类，也可以加载Maven依赖中包含的类)**
类加载器的加载路径可以通过classloader -c hash值查看



#### 双亲委派机制

由于java虚拟机中有多个类加载器，双亲委派机制的核心就是解决一个类到底由谁加载的问题

##### 一、双亲委派机制的作用
1. 保证类加载的安全性：通过双亲委派机制避免恶意代码替换JDK中的核心类库，比如java.lang.String
2. 避免重复加载：双亲委派机制可以避免同一个类被多次加载

双亲委派机制指的是：当一个类加载器接受到加载类的任务时，会自底向上查找是否加载过，再由顶向下进行加载。

先向上查找，（顺序为 应用程序类加载器 扩展类加载器 启动类加载器）如果该类已经加载过，就直接返回Class对象，加载过程结束

如果所有的父类加载器都无法加载该类，则由当前类加载器自己尝试加载，所以看上去是自顶向下尝试加载。

因此可以推断出， 如果自己定义一个java.lang.String 类，并不会被类加载器加载，因为启动类加载器会加载底层的 java.lang.String, 加载之后就会结束过程
<font color="#ffff00">也就是说 ，双亲委派机制会防止核心类被自定义类覆盖</font>

在Java中如何使用代码的方式去主动加载一个类呢？
方式一：用Class.forName方法，使用当前类的类加载器去加载指定的类
方式二：获取到类加载器，通过类加载器的loadClass方法指定某个类加载器的加载

因此 双亲委派机制是：
1. 当一个类加载器去加载某个类的时候，会自底向上查找是否加载过，如果加载过就直接返回，如果一直到最顶层的类加载器都没有加载，再由顶向下进行加载
2. 应用类加载器的父类加载器是扩展类加载器，扩展类加载器的父类加载器是启动类加载器
3. 有两个优点，第一是避免恶意代码替换JDK中的核心类库，确保核心类库的完整性和安全性。第二是避免一个类重复地被加载


##### 二、打破双亲委派机制
###### 1 自定义类加载器

一个Tomcat程序中是可以运行多个Web应用的，如果这两个应用中出现了相同限定名的类，比如Servlet类，Tomcat要保证这两个类都能加载并且他们应该是不同的类
如果不打破双亲委派机制，当应用类加载器加载Web应用1中的MyServlet之后，Web应用2中相同限定名的MyServlet类就无法被加载了

ClassLoader包含了四个核心方法

``` java
public Class<?> loadClass(String name) 是类加载的入口，提供了双亲委派机制，内部会调用findClass
```

```java
protected Class<?> findClass(String name) 由类加载器子类实现，获取二进制数据调用defineClass ，比如URLClassLoader会根据文件路径去获取类文件中的二进制数据
```

``` java
protected final Class<?> defineClass(String name, byte[] b, int off, int len) 做一些类名的校验，然后调用虚拟机底层的方法将字节码信息加载到虚拟机内存中
```

``` java 
// 双亲委派机制核心代码
// parent 等于null 说明父类加载器是启动类加载器，直接调用findBootstrapClassorNull
// 否则调用父类加载器的加载方法
if (parent != null) {  
c = parent.loadClass(name, false);  
} else {  
c = findBootstrapClassOrNull(name);  
}

// 如果发现父类加载器无法加载对应的类，只能自己去加载
if (c == null) {  
// If still not found, then invoke findClass in order  
// to find the class.  
c = findClass(name);  
}

```

<font color="#c0504d">问题一 自定义类加载器父类为什么是AppClassLoader</font>
答：
``` java
// 在构造时，可以通过parent参数选择父类加载器
private ClassLoader(Void unused, ClassLoader parent) {  
this.parent = parent;  
if (ParallelLoaders.isRegistered(this.getClass())) {  
parallelLockMap = new ConcurrentHashMap<>();  
package2certs = new ConcurrentHashMap<>();  
assertionLock = new Object();  
} else {  
// no finer-grained lock; lock on the classloader instance  
parallelLockMap = null;  
package2certs = new Hashtable<>();  
assertionLock = this;  
}  
}
```

``` java
protected ClassLoader() {  
this(checkCreateClassLoader(), getSystemClassLoader());  
}
```

getSystemClassLoader() 默认的父类加载器是AppClassLoader
这个方法调用了Launcher()构造器，这个构造器会返回 AppClassLoader

<font color="#c0504d">问题二 两个自定义类加载器加载相同限定的类名，会冲突吗？</font>

答：**不会冲突**，在同一个java虚拟机中，只有 <font color="#fdeada">相同类加载器 + 相同的类限定名</font> 才会被认为是同一个类
###### 2 线程上下文类加载器

举例：JDBC
JDBC中使用了DriverManager来管理项目中引入的不同数据库的驱动，比如mysql驱动，oracle驱动
DriverManager 属于 rt.jar， 是启动类加载器加载的。而用户jar包中的驱动需要由应用类加载器加载，这就违反了双亲委派机制

<font color="#d83931">问题一 DriverManager怎么知道jar包中要加载的驱动在哪？</font>
答：SPI机制
SPI的工作原理：
1 在ClassPath路径下的META-INF/services 文件夹中，以接口的全限定名来命名文件名，对应的文件里面写该接口的实现。 比如Mysql里就实现了java.sql.Driver， 并且将Driver对象 注册到DriverManager中
2 ServiceLoader加载实现类 
``` java
// 将要加载的接口进行加载，获取Driver对象
ServiceLoader<java.sql.Driver> loadedDrivers = ServiceLoader.load(java.sql.Driver.class)

```

<font color="#d83931">问题二 SPI中是如何获取到应用程序类加载器的？</font>
SPI中使用了线程上下文保存的类加载器进行类加载，这个类加载器一般是应用程序类加载器

[[JDBC线程上下文类加载器加载驱动类 2]]

<font color="#d83931">问题三 该案例真的打破了双亲委派机制吗？</font>

周志明 《深入理解Java虚拟机》中表示 是打破了双亲委派机制。
但是分析来看 JDBC只是在DriverManager加载完成后，通过初始化阶段触发了驱动类的加载，该加载仍然是遵循了双亲委派机制

###### 3 Osgi框架的类加载器

OSGi还使用类加载器实现了热部署的功能
热部署指的是在服务部停止的情况下，动态地更新字节码文件到内存中

如果代码上线后，发现有小bug，但是用户着急使用，如果新打包在发布需要一个多小时的时间，可以用arthas去解决该类问题
arthas热加载步骤
1. <font color="#548dd4">在出问题的服务器上部署arthas 并且启动</font>
2. <font color="#548dd4">jad --source-only 类全限定名 > 目录/文件名.java</font>
3. <font color="#548dd4">mc -c 类加载器的hashcode 目录/文件名.java -d 输出目录 （通过sc -d 全类名 去找到hashcode）</font>
4. <font color="#548dd4">retransform class文件所在目录/xxx.class</font>
<font color="#d83931">注意事项</font>
<font color="#d83931">1 程序重启之后，字节码文件会回复， 除非将class文件放入jar包中进行更新</font>
<font color="#d83931">2 使用retransform 不能添加方法或者字段，也不能更新正在执行中的方法</font>

#### JDK9之后的类加载器

JDK8以及之前的版本中，扩展类加载器和应用程序类加载器的源码位于rt.jar包中

JDK9 引入了module的概念，类加载器在设计上发生了很多变化
1 启动类加载器使用Java编写， 位于jdk.internal.loader.ClassLoaders类中。Java中的BootClassLoader 继承自BuiltinClassLoader 实现从模块中找到要加载的字节码资源文件
<font color="#d83931">启动类加载器依然无法通过java代码获取到，返回的仍然是null，保持了统一</font>


2 扩展类加载器被替换成了平台类加载器，平台类加载器遵循模块化方式加载字节码文件，所以继承关系从URLClassLoader 变成了 BuiltinClassLoader,  BuiltinClassLoader 实现了从模块中加载字节码文件。 平台类加载器的存在更多的是为了与老版本的设计方案兼容，自身没有特殊的逻辑

#### 总结

问题一 类加载器的作用
 类加载器负责类加载的过程中对字节码的获取，并且加载到内存中。通过加载字节码数据，将其放入内存饼转换成byte[], 接下来调用虚拟机底层方法将byte[] 转换成方法去和堆中的数据 

问题二 几种类加载器
1 启动类加载器 加载核心类
2 扩展类加载器 加载扩展类
3 应用程序类加载器 加载应用classpath中的类
4 自定义类加载器，重写findClass方法
JDK9之后 扩展类加载器变成了 平台类加载器

问题三 什么是双亲委派机制
问题四 怎么打破双亲委派机制

### 运行时的数据区域（JVM管理的内存）

java虚拟机在运行java程序过程中管理的内存区域，称之为运行时数据区

线程不共享
1 程序计数器
2 java虚拟机栈
3 本地方法栈

线程共享
1 方法区
2 堆

#### 程序计数器

程序计数器 也叫PC寄存器，每个线程会通过程序计数器记录当前要执行的字节码指令的地址
在代码执行过程中，<font color="#4bacc6">程序计数器会记录下一行字节码指令的地址</font>，执行完当前指令之后，虚拟机的执行引擎根据程序计数器执行下一行指令
在多线程执行的情况下，Java虚拟机需要通过程序计数器记录CPU切换前解释执行到那一句指令并继续执行

<font color="#d83931">问题 程序计数器在运行中会出现内存溢出吗</font>

内存溢出指的是程序在使用某一块内存区域时，存放的数据需要占用的内存大小超过了虚拟机能提供的内存上限
因为每个线程只存储一个固定长度的内存地址，程序计数器是不会发生内存溢出的
程序员无需对程序计数器做任何处理

#### 栈
##### java虚拟机栈
java虚拟机栈 采用站的数据结构来管理方法调用中的基本数据，先进后出，每个方法的调用使用一个栈帧（Stack Frame) 来保存
java虚拟机栈会随着线程的创建而创建，而回收则会在线程销毁时进行。由于方法可能会在不同线程中执行，每个线程都会包含一个自己的虚拟机栈

###### 栈帧的组成
1 局部变量表：实在运行过程中存放所有的局部变量
2 操作数栈：虚拟机在执行指令的过程中用来存放临时数据的一块区域
3 帧数据： 主要包含动态链接，方法出口，异常表的引用

1 局部变量表
1）栈帧中的局部变量表是一个数组，数组中每一个位置称为槽（slot），long和double类型占用两个槽，其他类型占用一个槽

2）实例方法中的序号为0的位置存放的时this，指的是当前调用方法的对象，**运行时会在内存中存放实例对象的地址**

3）方法参数也会保存在局部变量表中，其顺序与方法中参数定义的顺序一致
4）局部变量表保存的内容有：实例方法的this对象，方法的参数，方法体中声明的局部变量
5）为了节省空间，局部变量表中的槽是可以服用的，一旦某个局部变量不再生效，当前槽就可以再次被使用
举例 如下代码用了几个槽
``` java
public void test() {  
{  
int a = 1;  
int b = 2;  
}  
{  
int c = 1;  
}  
int i = 0;  
long j = 1;  
}
```

<font color="#b8cce4">最大槽数为6</font>
<font color="#b8cce4">this, k, m, i, j, j</font>
<font color="#b8cce4">也是因此在代码块外就无法再去访问 a b c 变量了</font>

2 操作数栈
3 帧数据
1） 当前类的字节码指令引用了其他类的属性或者方法时，需要将符号引用（编号）转换成对应的运行时常量池中的内存地址
[[帧数据]]

2）方法出口指的是方法在正确或者一场结束时，当前栈帧会被弹出，<font color="#ff0000">同时程序计数器应该指向上一个栈帧中的下一条指令的地址。</font> 因此在当前栈帧中，需要存储方法出口的地址。

3）异常表存放的是代码中异常的处理信息，包含了异常捕获的生效范围一集异常发生后跳转到的字节码指令位置

| Nr. | 起始PC | 结束PC | 跳转PC | 捕获类型                |
| --- | ---- | ---- | ---- | ------------------- |
| 0   | 2    | 4    | 7    | java/lang/Exception |
```
0 iconst_0
1 istore_0
2 iconst_1
3 istore_0
4 goto 10 (+6)
7 astore_1
8 iconst_2
9 istore_0
10 return
```

```java
public void test() {  
int i = 0;  
try {  
i = 1;  
} catch (Exception e) {  
i = 2;  
}  
}
```
###### 栈内存的溢出

1 java虚拟机栈如果栈帧过多，占用内存超过栈内存可以分配的最大内存就会出现内存溢出，出现StackOverFlow

2 如果我们不指定栈的大小，JVM将创建一个具有默认大小的栈，大小取决于操作系统和计算机的体系结构
Linux ：x86 1MB   ppc 2MB
Windows: 基于操作系统的默认值                           

	1）修改Java虚拟机栈大小，可以使用虚拟机参数 -Xss
	单位：字节（默认，必须是1024倍数）, k或者K(KB), m或者M(MB), g或者G（GB）
	
	-Xss1048576
	-Xss1024K
	-Xss1m
	-Xss1g
	
	2）与-Xss类似，也可以使用 -XX:ThreadStackSize调整标志来配置堆栈大小
	格式为：-XX:ThreadStackSize=1024
	
	HotSpot JVM对栈大小的最大值和最小值有要求：
	Windows(64位) 下的JDK8测试最小值为180k, 最大值为1024m

	3）局部变量过多，操作数栈深度过大也会影响栈内存大小

##### 本地方法栈
本地方法栈存储的是native本地方法的栈空间
<font color="#ff0000">在HotSpot虚拟机中，Java虚拟机栈和本地方法栈是线上使用了同一个栈空间</font>

#### 堆

![[方法区和堆的关系.png]]

1 一般java程序中堆内存是空间最大的一块内存区域，创建出来的对象都存在与堆上
2 栈上的局部变量表中，可以存放堆上对象的引用（即内存地址）。静态变量也可以存放堆对象的引用，通过静态变量就可以实现对象在线程之间共享。
3 堆内存的大小是有限的，当对象一直向堆中放入对象达到上限之后，就会抛出outOfMemory
4 堆空间有三个需要关注的值：used 指的是当前已经使用的内存，total是java虚拟机已经分配的可用堆内存，max是java虚拟机可以分配的最大堆内存

Memory                    used    total    max     usage    GC
heap                      44M     303M     7241M   0.61%    gc.ps_scavenge.count          1
ps_eden_space             20M     128M     2673M   0.78%    gc.ps_scavenge.time(ms)       7
ps_survivor_space         0K      21504K   21504K  0.00%    gc.ps_marksweep.count         1
ps_old_gen                23M     154M     5431M   0.43%    gc.ps_marksweep.time(ms)      28
nonheap                   32M     32M      -1      97.70%
code_cache                7M      7M       240M    3.26%
metaspace                 21M     22M      -1      97.62%
compressed_class_space    2M      2M       1024M   0.26%

随着堆中的对象增多，当total可以使用的内存即将不足时，java虚拟机会继续分配内存给堆

<font color="#ff0000">问题 是不是当used=total=max 堆内存就会溢出了呢？</font>
答：不是，堆内存溢出的判断条件很复杂，和垃圾回收器有关

如果不设置任何的虚拟机参数，max默认是系统内存的1/4，total默认是系统内存的1/64。 在实际应用中一般都需要设置total和max的值

total的初始值设置 是 -Xms值
max的值设置 是 -Xmx值

单位：字节（默认，必须是1024倍数）, k或者K(KB), m或者M(MB), g或者G（GB）
限制：Xmx必须大于2MB，Xms必须大于1MB

<font color="#ff0000">问题 为什么arthas中显示的heap堆大小与设置的不一样</font>
答：arthas中的堆内存使用了JMX技术中内存获取方式，这种方式与垃圾回收器有关，<font color="#ffc000">计算的是可以分配的对象内存，而不是整个内存</font> 

建议：将-Xmx 和 -Xms设置为相同值，这样就不需要再向java虚拟机再次申请，减少了申请并分配内存时间上的开销，同事也不会出现内存过剩之后堆收缩的情况
-Xmx具体设置的值与实际的应用程序运行环境有关

#### 方法区

方法区是存放基础信息的位置，线程共享，主要包含
1 类的元信息 保存了所有类的基本信息
2 运行时常量池 保存了字节码文件中的常量池内容
3 字符串常量池 保存了字符串常量

##### 类的元信息

方法区是用来存储每个类的基本信息（元信息），一般称之为InstanceKlass对象，在类的加载阶段完成
java虚拟机在实现方法上会把常量池和方法会用一块单独的内存区域去存放，InstanceKlass只是存放了他们的引用

##### 运行时常量池

方法区处理存储类的元信息之外，还存放了运行时常量池。常量池中存放的是字节码中的常量池内容
字节码文件中通过编号查表的方式找到常量，这种常量池称为静态常量池。当常量池加载到内存中之后，可以通过内存地址快速的定位到常量池中的内容，这种常量池称为运行时常量池

JDK7及之前 方法区存放在堆区中的永久代空间，堆的大小由虚拟机参数来控制
JDK8及之后 方法区存放在元空间(metaSpace)中，元空间位于操作系统维护的直接内存中，默认情况下只要不超过操作系统承受的上限，就可以一直分配

JDK7运行十几万次就会出错，而在JDK8上运行百万次没程序也没错误，但是内存会直线升高

JDK7将方法区存放在堆区域中的永久代空间，堆的大小由虚拟机参数-XX:MaxPermSize=值 来控制
JDK8 将方法区存放在元空间中，元空间位于操作系统维护的直接内存中，默认情况下只要不超过操作系统承受的上线，就可以一直分配。可以使用-XX:MaxMetaspaceSize=值 将元空间最大大小进行限制

#### 字符串常量池
方法区中处理类的元信息，运行时常量池之外，还有一块区域叫字符串常量池
字符串常量池存储在代码中定义的常量字符串

问题 字符串常量池和运行时常量池有什么关系
早期设计时，字符串常量池时属于运行时常量池的一部分，他们存储的位置也是一致的。后续做出了调整，将字符串常量池和运行时常量池做了拆分


``` java
public static void main(String[] args) throws ClassNotFoundException, IOException {  
String a = "1";  
String b = "2";  
String c = "12";  
String d = a + b;  
System.out.println(c == d);  
}
```

```
 0 ldc #2 <1>
 2 astore_1
 3 ldc #3 <2>
 5 astore_2
 6 ldc #4 <12>
 8 astore_3
 9 new #5 <java/lang/StringBuilder>
12 dup
13 invokespecial #6 <java/lang/StringBuilder.<init> : ()V>
16 aload_1
17 invokevirtual #7 <java/lang/StringBuilder.append : (Ljava/lang/String;)Ljava/lang/StringBuilder;>
20 aload_2
21 invokevirtual #7 <java/lang/StringBuilder.append : (Ljava/lang/String;)Ljava/lang/StringBuilder;>
24 invokevirtual #8 <java/lang/StringBuilder.toString : ()Ljava/lang/String;>
27 astore 4
29 getstatic #9 <java/lang/System.out : Ljava/io/PrintStream;>
32 aload_3
33 aload 4
35 if_acmpne 42 (+7)
38 iconst_1
39 goto 43 (+4)
42 iconst_0
43 invokevirtual #10 <java/io/PrintStream.println : (Z)V>
46 return

```

结果输出为false 通过aload指令拿到1 和 2 ，在用StringBuilder的append指令拼接
[[字符串常量池 题目]]

JDK7之后，由于字符串常量池在堆上，所以intern()方法会把第一次遇到的字符串的引用放入字符串常量池 

#### 直接内存

1 Java堆中的对象如果不再使用要回收，回收时会影响对象的创建和使用
2 IO操作比如读文件，需要先把文件读入直接内存（缓冲区），再把数据复制到Java堆中。NIO的话，可以直接放入直接内存，同时在java堆上维护直接内存的引用，减少了数据复制的开销。

如果需要手动调整直接内存大小，可以使用-XX:MaxDirectMemorySize=大小


## 垃圾回收机制

内存泄漏指的是 ：不再使用的对象在系统中未被回收。内存泄露的积累可能会导致内存溢出

java中为了简化对象的释放，引入了自动的垃圾回收机制。通过垃圾回收器来对不再使用的对象完成自动的回收，垃圾回收器主要负责对堆上的内存进行回收。

应用场景：
1 解决系统僵死的问题（可能与频繁的垃圾回收有关）
2 性能优化（对垃圾回收器进行合理的设置可以有效地提升程序的执行性能）

#### 方法区的回收

线程不共享的部分，都是伴随着线程的创建而创建，线程的销毁而销毁。而方法的栈帧在执行完方法之后就会自动弹出栈并且释放掉对应的内存
判断一个类可以被卸载 需要同时满足下面三个条件
1 此类所有的的实例对象都已经被回收，在堆中不存在任何该类的实例对象一集子类对象
2 加载该类的类加载器已经被回收
3 该类对应的java.lang.Class 对象没有在任何地方被使用

如果需要手动触发垃圾回收，可以调用System.gc()方法
调用System.gc()方法不一定会立即回收垃圾，仅仅是向Java虚拟机发送一个垃圾回收的请求，具体是否需要执行垃圾回收，Java虚拟机会自行判断。

-XX:TraceClassLoading -XX:TraceClassUnloading 打印出类的加载和卸载

在OSGi,JSP等热部署场景中，每个jsp文件对应一个唯一的类加载器，当一个jsp文件修改了，就直接卸载这个jsp类加载器。重新创建类加载器，重新加载jsp文件


#### 堆回收

##### 引用计数法和可达性分析法

如何判断堆上的对象可以回收？
答：Java中的对象是否能被回收，是根据对象是否被引用来决定的。如果对象被引用了，说明该对象在使用，不允许被回收。**（此说法不严谨）**

常见的有两种判断 堆上的对象是否被引用 的方法：
###### 一 引用计数法

缺点：
1 每次引用和取消引用都需要维护计数器，对性能会有一定的影响
2 存在循环引用问题，所谓循环引用就是当A引用B，B同时引用A时会出现对象无法回收的问题
``` java
A a1 = new A();
B b1 = new B();
a1.b = b1;
b1.a = a1;
a1 = null;
b1 = null;
```

由上面代码可以看出 断掉栈内存里 a1 b1 对于堆内存对应的实例对象的引用，但由于 a b 还在循环引用，因此会导致内存泄露

如果需要查看垃圾回收的信息，可以使用 -verbose:gc参数
语法 ： -verbose:gc
###### 二 可达性分析法
Java 使用的时可达性分析算法来判断对象是否可以被回收。可达性分析将对象分为两类：垃圾回收的跟对象(GC Root) 和普通对象，对象与对象之间存在引用关系
[[可达性分析算法示意图]]

A到B再到C和D，形成了一个引用链，可达性分析算法指的是如果从某个对象到GC Root是可达的，该对象就不会被回收

GC Root对象有如下几种类型：
1 线程Thread对象，引用线程栈帧中的方法参数，局部变量等
2 系统类加载器加载的java.lang.Class对象，引用类中的静态变量
3 监视器对象，用来保存同步锁synchronized 关键字持有的对象
4 本地方法调用时使用的全局对象

##### 五种对象引用

可达性算法中描述的对象引用，一般指的时强引用，即时GC Root对象堆普通对象有引用关系，只要这层关系存在，普通对象既不会被回收，除了强引用之外，java中还设计了几种其他引用方式

1 软引用
2 弱引用
3 虚引用
4 终结器引用

###### 一 软引用

软引用相较于强引用是一种比较弱的引用关系，如果一个对象只有一个软引用关联到它，当程序内存不足时，就会将软引用中的数据进行回收

软引用执行过程中如下：
1 将对象使用软引用包装起来，new SoftReference<对象类型>(对象)
2 内存不足时，虚拟机尝试进行垃圾回收
3 如果垃圾回收仍不能解决内存不足的问题，回收软引用中的对象
4 如果依然内存不足，抛出OutOfMemory

软引用中的对象如果在内存不足时回收， SoftReference对象本身也需要被回收，那如何知道哪些SoftReference对象需要回收呢？
SoftReference提供了一套队列机制
1 软引用创建时，通过构造器传入引用队列 
2 在软引用中包含的对象被回收时，该软引用对象会被放入引用队列中
3 通过代码遍历引用队列，将SoftReference的强引用删除

软引用也可以使用继承自SoftReference类的方式来实现

###### 二 弱引用

弱引用的机制和软引用基本一致，区别在于弱引用包含的对象在垃圾回收时，不管内存够不够都会直接被回收, **弱引用主要在ThreadLocal中使用**

``` java
public static void main(String[] args) {  
byte[] bytes = new byte[1024 * 1024 * 100];  
WeakReference<byte[]> weakReference = new WeakReference<>(bytes);  
bytes = null; // 解除强引用  
System.out.println(weakReference.get());  
  
System.gc();  
System.out.println(weakReference.get());  
}
```

###### 三 虚引用

1 在常规开发中不会使用

2 也叫做幽灵引用/幻影引用，不能通过虚引用对象获取到包含的对象。虚引用唯一的用途时当对象被垃圾回收器回收时可以接收到对应的通知。Java中使用PhantomReference实现了虚引用，直接内存中为了及时知道直接内存对象不再使用，从而回收内存，使用了虚引用来实现
###### 四 终结器引用

1 在常规开发中不会使用


##### 垃圾回收算法

###### 核心思想
1 找到内存中存活的对象
2 释放不再存活对象的内存，使得程序能再次利用这部分空间

算法分类
1 标记-清除算法
2 复制算法
3 标记-整理算法
4 分代GC

Java垃圾回收过程会通过单独的GC线程来完成，但是不管使用哪一种算法，都会有部分阶段需要停止所有的用户线程。这个过程被称为STW(Stop the world), 如果STW时间过长则会影响用户使用

GC算法是否优秀可以从三个方面考虑
1 吞吐量
指的是CPU用于执行用户代码的时间与CPU总执行时间的比值，即吞吐量 = 执行用户代码时间 / （执行用户代码时间 + GC 时间）。吞吐量数值越高，垃圾回收的效率越高。

2 最大暂停时间
最大暂停时间指的是所有再垃圾回收过程中的STW时间的最大值。最大暂停时间越短，用户使用系统时受到的影响就越短

3 堆使用效率
不同的垃圾回收算法，对堆内存使用的方式是不同的。比如标记清除算法，可以使用完整的堆内存。而复制算法会将堆内存一分为二，每次只能使用一半内存。因此从堆使用效率来说，标记清除算法要优于复制算法。

一般这三种不可兼得。堆内存越大，最大暂停时间就越长，要想减少最大暂停时间，就会降低吞吐量

<font color="#ff0000">最大暂停时间和吞吐量的关系</font>

1. **相互竞争**：<font color="#ff0000">高吞吐量和低最大暂停时间往往是一对相互竞争的目标。为了实现高吞吐量，GC线程可能会减少执行频率，但每次执行时需要更长的时间来完成内存回收，从而导致较长的暂停时间。相反，为了降低最大暂停时间，GC线程需要更频繁地执行，但这会增加GC的总开销并降低吞吐量。</font>
2. **折衷选择**：在设计或使用GC算法时，必须根据应用程序的具体需求来确定是优先追求高吞吐量还是低最大暂停时间。有些算法（如G1 GC）试图在两者之间找到一个平衡点，以同时满足对吞吐量和暂停时间的需求。
3. **配置调整**：通过JVM的启动参数（如-XX:MaxGCPauseMillis和-XX:GCTimeRatio），可以对GC的行为进行配置，以在一定程度上调整吞吐量和最大暂停时间之间的平衡。然而，这些配置通常需要根据具体的应用程序和硬件环境进行调优。

###### 标记清除算法

一 过程
1 标记阶段，将所有存活的对象进行标记。Java中使用可达性分析算法，从GC Root开始通过引用链遍历出所有存活对象
2 清楚阶段，在内存中删除没有被标记的对象，也就是非存活对象。

二 优点
实现简单，只需要在标记阶段给每个对象维护标志位，第二阶段删除对象即可

三 缺点
1 碎片化问题，由于内存是连续的，所以在对象被删除之后，内存中会出现很多细小的可用内存单元。如果我们需要的是一个比较大的空间，很有可能这些内存单元的大小过小无法进行分配
2 分配速度慢。由于内存碎片的存在，需要维护一个空闲链表，极有可能发生每次需要遍历到链表最后才能获得合适的内存空间

###### 复制算法

一 过程
1 分为两块空间：From 和 To， 每次在对象分配阶段，只能使用其中一块空间（From）
2 在垃圾回收GC阶段，将From中存活的对象复制到To空间
3 将两块空间的From和To名字互换

二 优点
1 吞吐量高
复制算法只需要便利一次存货对象复制到To空间即可，比标记-整理算法少了一次遍历的过程，因而性能较好，但是不如标记-清除算法，因为标记清除算法不需要对象的移动

2 不会发生碎片化
复制算法在复制之后就会将对象按照顺序放入To空间，所以对象以外的区域都是可用空间，不存在碎片化内存空间

三 缺点
内存使用效率低
每次只能让一般的内存空间来为创建对象使用

###### 标记整理算法

也叫标记压缩算法，是对标记清理算法中容易产生碎片问题的一种解决方案

一 过程
1 标记阶段，将所有存活的对象进行标记。 java中使用可达性分析算法，从GC Root开始通过引用链遍历出所有存活对象

2 整理阶段，将存活对象移动到堆的一端。清理掉存活对象的内存空间

二 优点
1 内存使用率高
整个堆内存都可以使用，不会像复制算法只能使用半个堆内存

2 不会发生碎片化
在整理阶段可以将对象往内存的一侧进行移动，剩下的空间都是可以分配对象的有效空间

三 缺点
整理算法有很多种， 比如 Lisp2 整理算法，需要对整个堆中的对象搜索三次，整体性能不佳。可以通过Two-finger , ImmixGC等高效的整理算法优化此阶段性能

###### 分代垃圾回收算法

分代垃圾回收将整个内存区域划分为年轻代和老年代

[[分代GC内存]]


| 参数名                           | 参数含义                                                | 示例                  |
| ----------------------------- | --------------------------------------------------- | ------------------- |
| -Xms                          | 设置堆的最小和初始大小，必须是1024的倍数且大于1MB                        | -Xms6144k           |
| -Xmx                          | 设置最大堆的大小，必须是1024倍数且大于2MB                            | -Xmx81920k          |
| -Xmn                          | 新生代大小                                               | -Xmn262144k         |
| -XX:SurvivorRatio             | 伊甸园区和幸存区的比例，默认为8，<br>新生代1g内存，那么伊甸园区800MB，S0和S1各100M | -XX:SurvivorRatio=4 |
| -XX:+PrintGCDetailsverbose:gc | 打印gc日志                                              |                     |

Minor GC 回收新生代
如果Minor GC 后对象的年龄达到阈值（最大15，默认值和垃圾回收器有关）,对象会被晋升为老年代
当老年代中空间不足，无法放入新的对象是，先尝试minor GC， 如果还是不足， 就会触发Full GC， Full GC 会对整个堆进行垃圾回收，如果Full GC依然无法回收掉老年代的对象，那么当对象继续放入老年代时，就会抛出Out Of Memory异常

<font color="#ff0000">问题 为什么GC算法要把堆分为年轻代和老年代</font>
 分析：
 1 系统中大部分对象，都是创建出来之后很快就不再使用了，就可以被回收。比如用户获取订单数据，订单数据返回给用户之后就可以释放了
 2 老年代中会存放长期存活的对象，比如Spring中大部分bean对象，在程序启动之后就不会被回收了
 3 在虚拟机的默认设置中，新生代大小要远小于老年代大小


答：
1可以通过调整年轻代和老年代的比例来适应不同类型的应用程序，提高内存利用率和性能
2 新生代和老年代可以用不同的垃圾回收算法：新生代一般用复制算法，老年代可以选择 标记清除 或者 标记整理法
3 分代的设计中只允许回收新生代，如果能满足对象分配的要求就不要对整个堆进行回收，STW时间就会减少
[[垃圾回收器组合]]

组合一 
年轻代Serial垃圾回收器
Serial是一种单线程串行回收年轻代的垃圾回收器
优点
单CPU处理器下吞吐量非常出色
缺点
多CPU下吞吐量不如其他垃圾回收器，堆如果偏大会让用户线程处于长时间等待

老年代SerialOld垃圾回收器
采用单线程串行回收
-XX:+UserSerialGC 新生代，老年代都使用串行回收器

组合二
年轻代 ParNew垃圾回收器
使用多线程进行垃圾回收
-XX:UseParNewGC 新生代使用ParNew回收器，老年代使用串行回收器

老年代 -CMS（Concurrent Mark Sweep）垃圾回收器
CMS垃圾回收器关注的是系统的暂停时间，允许用户线程和垃圾回收器线程在某些步骤中同时执行，减少了用户线程的等待时间
参数： -XX:+UseConcMarkSweepGC
优点：
由于垃圾回收出现的停顿时间短，用户体验好
缺点：
1 内存碎片问题
2 退化问题
3 浮动垃圾问题
适用场景：
大型的互联网系统中用户请求数据量答，频率高的场景，比如订单接口，商品接口等

CMS执行步骤
1 初始标记，用极短的时间标记出GC Roots能直接关联到的对象，存在STW
2 并发标记，标记所有的对象（哪些对象需要回收，哪些对象不需要回收），用户线程不需要暂停
3 重新标记，由于并发标记阶段有些对象会发生了变化，存在错标，漏标等情况，需要重新标记。存在STW
4 并发清理，清理死亡的对象，用户线程不需要暂停

CMS缺点：
1 使用了标记-清除法，在垃圾收集结束之后会出现大量的内存碎片，CMS会在Full GC时进行碎片整理。会导致用户线程暂停，可以使用-XX:CMSFullGCsBeforeCompaction = N 参数（默认0） 调整N次 Full GC之后再整理
2 无法处理在并发清理过程中产生的 “浮动垃圾” ，比如在清理时，用户线程又创建了对象。 不能做到完全的垃圾回收。
3 如果老年代内存不足，无法分配对象，CMS就会退化成Serial Old单线程回收老年代

组合三
年轻代 PS垃圾回收器
PS是JSK8默认的年轻代垃圾回收器，多线程并行回收，关注的是系统的吞吐量。具备自动调整堆内存大小的特点
涉及算法：复制算法
优点：吞吐量高，而且手动可控。为了提高吞吐量，虚拟机会动态调整堆的参数
缺点：不能保证单次的停顿时间

老年代 PO垃圾回收器
参数：-XX:UserParallelGC 可以使用 PS + PO 这种组合
优点：并发收集，在多核CPU下效率高
缺点：暂停时间长

###### G1垃圾回收器
PS关注吞吐量，允许用户设置最大暂停时间，但是会减少年轻代可用空间大小
CMS关注暂停时间，但是吞吐量方面会下降
G1设计目标就是将上述两种垃圾回收器有点融合
1 支持巨大的堆空间回收，并有较高吞吐量
2 支持多CPU并行垃圾回收
3 允许用户设置最大暂停时间

G1垃圾回收器-内存结构
整个堆会被划分成多个大小相等的区域，称之为Region，区域不要求是连续的。分为Eden，Survivor，Old区。Region的大小通过 堆空间大小 / 2048得到 ，也可以通过参数 -XX:G1HeapRegionSize=32m指定，Region size必须是2的指数幂， 取值范围从1M到32M

G1垃圾回收有两种方式
一 年轻代回收
回收Eden区和Survivor区中不用的对象。会导致STW，G1中可以通过参数 -XX:MaxGCPauseMillis=n (默认200) 设置每次垃圾回收时的最大暂停时间毫秒数，G1垃圾回收器会尽可能地保证暂停时间

执行流程
1 新创建对象会存放在Eden区， 当G1判断年轻代区（即 Eden + Survivor）已经超过堆内存的60%，无法分配对象需要回收时，会执行Young GC
2 标记出Eden和Survivor区域中存活的对象
3 根据配置的最大暂停时间选择某些区域将存活对象复制到一个新的Survivor中，并且年龄 + 1 ，然后清空这些区域。
4 后续Young GC时与之前相同，只不过Survivor区域中存活对象会被搬运到另一个Survivor
5 当某个存活对象的年龄达到阈值（默认15），将被放入老年代
6 部分对象如果大小超过Region的一般，会直接放入老年代，这类老年代被称为Humongous区。比如堆内存是4G，每个Region就是2M，只要一个大对象超过了1M就会被放入Humongous区，如果对象过大会横跨多个Region
7 多次回收之后，会出现很多Old老年代区，此时总堆占有率达到阈值时(-XX:InitiatingHeapOccupancyPercent默认为45%) 会触发混合回收MixedGC。回收所有年轻代和部分老年代的对象以及大对象区。采用复制算法来完成

二 混合回收
1 初始标记 标记为GC Roots引用的对象为存活
2 并发标记 将第一步中标记的对象引用的对象，并且标记为存活
3 最终标记 由于在并发标记中，标记线程和用户线程并发执行，所以可能会有些漏标，错标的情况，因此这里会标记一些引用改变漏标的对象，不管新创建，不再关联的对象
4 并发清理 将存活的对象复制到别的Region中，不会产生内存碎片（使用复制算法）

G1对老年代的清理会选择存活度最低的区域来进行回收，这样可以保证回收效率最高


## 内存调优

### 内存溢出和内存泄漏

内存泄漏 （memory leak）：
在Java中如果不在使用一个对象，但是该对象依然在GCRoot的引用链上，这个对象就不会被垃圾回收，这种情况就称之为内存泄漏

少量的内存泄漏可以容忍，但是如果发生持续的内存泄漏，就会像雪球一样越滚越大，不管有多大的内存都会被消耗完，最终导致内存溢出。但是产生内存溢出并不是只有内存泄漏这一种原因

内存泄漏场景
一 在处理用户的请求之后，没有及时将用户的数据删除。随着用户请求越来越多，内存泄漏的对象沾满了堆内存最终导致溢出
这种产生的内存溢出会直接导致用户请求无法处理，影响用户的正常使用。重启虽然可以恢复使用，但是再运行一段时间之后仍然会出现内存溢出


二 分布式任务调度系统进行任务调度时，被调度的Java应用再调度任务结束中出现了内存泄漏，最终导致多次调度之后内存溢出
这种产生的内存溢出会直接导致用户请求无法处理，影响用户的正常使用。重启虽然可以恢复使用，但是再运行一段时间之后仍然会出现内存溢出


### 解决内存溢出的方法

发现 诊断 修复 验证

#### top命令

load average: 0.02 0.10 0.06 过去一分钟 5分钟 15分钟的 cpu的负载，超过1就表示使用量很大
RES(常驻内存) 代表整个进程使用多少内存
SHR(共享内存)

只能看到最基础的进程信息，无法查看每个部分的内存占用

#### VisualVM

#### Arthas tunnel

#### Prometheus + Grafana

![[堆内存对比.png]]


### 内存溢出原因

#### 原因一 代码中的内存泄漏

1 equals 和 hashCode 不正确的equals和 hashCode实现导致的内存泄漏

2 内部类引用外部类<font color="#ff0000"> 非静态的内部类和匿名内部类</font>的错误使用导致内存泄漏，所以使用静态

3 ThreadLocal的使用 由于线程池的线程不被回收导致的ThreadLocal内存泄漏

如果仅仅使用手动创建的线程，就算没有调用ThreadLocal的remove方法清理数据，也不会产生内存泄漏。因为当线程被回收时，ThreadLocal也同样被回收。但是如果用线程池就不一定了

4 String的intern方法

5 通过静态字段保存对象 大量的数据再静态变量中被引用，但是不再使用，成为内存泄漏
可以通过减少将对象长时间的保存在静态变量中的，如果不再使用，必须将对象删除或者将将静态变量设置为null
使用单例模式，尽量使用懒加载，而不是立即加载
Spring的Bean中不要长期存放大对象，如果是缓存用于提升性能，尽量设置过期时间定期失效

6 资源没有正常关闭
1 为了防止出现这类的资源对象泄漏，必须在finally块中关闭不再使用的资源
2 从java7开始 使用 try-with-resources 语法可以用于自动关闭资源

``` java
  

try (ResourceType1 resource1 = new ResourceType1(); ResourceType2 resource2 = new ResourceType2()) { // 使用资源进行操作 } catch (ExceptionType1 e) { // 处理异常 }

// 在这个结构中，`ResourceType1`和`ResourceType2`是需要自动关闭的资源类型，它们必须实现`AutoCloseable`接口。当`try`块执行完毕后，JVM会自动调用这些资源的`close()`方法来关闭它们，无论是否发生异常都会执行。如果在`try`块或`close()`方法中发生异常，只有`try`块中的异常会被抛出，而`close()`方法中的异常会被压制。‌

```

#### 原因二 并发请求问题

并发请求问题指的是用户通过发送请求向Java应用获取数据，正常情况下Java应用将数据返回之后，这部分数据就可以在内存中被释放掉。但是由于用户的并发请求量有可能很大，同时处理数据的时间很长，导致大量的数据存在于内存中，最终超过了内存的上线，导致内存溢出

### 诊断

#### 内存快照

生成内存快照的Java虚拟机参数：
-XX:+HeapDumpOnOutOfMemoryError: 发生OutOfMemoryError错误时，自动生成hprof内存快照文件
-XX:HeapDumpPath=/<path/>:指定hprof文件的输出路径


## GC调优

对垃圾回收进行调优。GC调优的主要目标是避免垃圾回收引起的程序性能下降

一 GC调优的核心分为三部分
1 Jvm参数的设置
2 特定垃圾回收器的Jvm参数的设置
3 解决由频繁的FullGc引起的程序性能问题

二 核心指标
1 垃圾回收吞吐量
2 延迟
3 内存使用量指的是Java应用占用系统内存的最大值，一般通过Jvm参数调整。在满足前两个指标的前提下，内存使用量越小越好

三 发现问题

jstat 工具是JDK自带的一款监控工具，可以提供各种垃圾回收，类加载，编译信息等不同数据 
jstat -gc 进程id  每次统计的间隔 统计次数

C代表capacity 容量， U 代表 Used使用量 S 幸存者区 E 伊甸园区 O 老年代 M 元空间

### 垃圾回收器选择

#### 一 优化基础JVM参数

#### 二 减少对象产生

#### 三 更换垃圾回收器

#### 四 优化垃圾回收器参数
